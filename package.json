{
  "name": "restaurant-site-root",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client && cp -R client/dist dist"
  }
}
